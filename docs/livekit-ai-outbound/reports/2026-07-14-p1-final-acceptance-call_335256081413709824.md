# 2026-07-14 P1 final acceptance: call_335256081413709824

## Scope

This is the final evidence pass for the current SIP barge-in P1 tuning cycle.
It evaluates one real acceptance call against the clean 19011 runtime and does
not change runtime authority rules, detector thresholds, or the 19012 service.

- Runtime worktree: `/Users/liuhongli/.codex/worktrees/p1-acceptance-8ffa0b9/ai-call`
- Runtime commit: `8ffa0b9bd903d68df5e5b4662c1966aacbfc69e5`
- Call ID: `call_335256081413709824`
- Call status: `completed`
- End reason: `web_user_end`
- Duration: `84583ms`

The clean-worktree freeze suite passed before the call: main matrix `23/23`,
local audio matrix `4/4`, focused tests `56`, and Phase A focused tests `17`.

## Live event result

`tools/ai_call_p1_eval.py` reports `quality.passed=false`.

| outcome | count |
| --- | ---: |
| confirmed pre-stop | 2 |
| confirmed without pre-stop | 2 |
| false pre-stop | 1 |
| candidate only | 4 |
| provider speech started | 10 |

The two successful local pre-stops took `185ms` and `78ms` from candidate to
pre-stop. The false pre-stop took `21ms`, then resolved as `rejected_noise`
after `500ms`.

Offline customer-track ASR found four customer spans:

| customer text | audio offset | live candidate lag | live pre-stop result |
| --- | ---: | ---: | --- |
| `你好，你好方便。` | `3280-10740ms` | `916ms` | no fast pre-stop |
| `好的好的。` | `26900-31800ms` | `1038ms` | `4103ms` from ASR span start |
| `行。` | `40220-41920ms` | `1016ms` | no fast pre-stop |
| `可以下次再说吧。` | `56840-60280ms` | `1103ms` | provider-confirmed without local pre-stop |

The first three spans fail the evaluator's `500ms` speech-start-to-pre-stop
gate. The first and third were deferred by the AI playback echo guard. The
second first deferred with `awaiting_pre_stop_authority`, then a later candidate
pre-stopped the response.

The call did not contain a recognized explicit customer hangup phrase such as
`挂了吧`, so the commit's explicit `customer_end` fast path was not exercised.
The model later requested `task_completed`; a following customer utterance
interrupted that ending, and the browser finally ended the call.

## Detector comparison

Timeline-aware replay reconstructed `7` interruptible windows covering
`67403ms` of the customer track.

| path | candidates | detector pre-stops | first candidate | first pre-stop |
| --- | ---: | ---: | ---: | ---: |
| live events | 9 | 3 | `4196ms` | `31003ms` |
| WebRTC, live timeline | 15 | 6 | `3880ms` | `3940ms` |
| FSMN, live timeline | 6 | 3 | `3920ms` | `7060ms` |
| WebRTC, always interruptible | 17 | 6 | `3880ms` | `3940ms` |
| FSMN, always interruptible | 11 | 5 | `3920ms` | `7060ms` |

FSMN does not provide a general safe shortcut. It finds fewer timeline-aware
candidates than WebRTC and still misses the short `行` window in the live
timeline. Replacing the detector would not by itself fix the post-candidate
authority delays or the live false pre-stop.

## Audio fixtures

Five local-only replay fixtures were exported under:

`/tmp/ai-call-final-acceptance-335256081413709824`

They include four customer speech slices plus the `49400-51400ms` rejected
noise/echo slice, with aligned AI audio. No recording or audio file is added to
Git.

Detector-only replay passes three of four speech slices; only the short `行`
slice has no pre-stop. Clean-commit audio authority replay produces:

| fixture | expectation | result |
| --- | --- | --- |
| opening `你好，你好方便` | must interrupt | FAIL, missing pre-stop |
| `好的好的` | must interrupt | PASS, `220ms` |
| short `行` | must interrupt | FAIL, missing pre-stop |
| `可以下次再说吧` | must interrupt | PASS, `220ms` |
| rejected noise/echo | must not interrupt | PASS |

The isolated authority replay is not bit-exact runtime reconstruction, but it
confirms two separate limits: opening/short-speech coverage remains incomplete,
and at least one live multi-second delay depends on runtime turn/echo context
beyond the raw customer audio.

## Closure decision

The final acceptance result is **FAIL for product-done** and **PASS for
freeze-ready**.

1. Freeze `8ffa0b9` as the bounded P1 baseline; do not promote the dirty
   high-noise experiments in the main worktree.
2. Stop adding authority branches or tuning thresholds from individual calls.
3. Keep FSMN and WebRTC/FSMN agreement offline or shadow-only.
4. Close the current P1 tuning cycle with the known limitation that conservative
   echo protection can miss opening and short Chinese interruptions.
5. Reopen implementation only as a separate evidence-provider change. Its gate
   must make the opening and short-speech fixtures pass while retaining the
   rejected-noise pass and producing zero false pre-stops on repeated real calls.

This is the stop condition. The current evidence does not justify another live
rule patch.
